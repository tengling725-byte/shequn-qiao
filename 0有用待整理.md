帮我审查这段代码，找出代码坏味道：高耦合、硬编码、分支泛滥、职责混杂，
结合SOLID原则，选用合适设计模式轻量化重构，拒绝过度设计，保留原有业务逻辑。

# 【AI 编程专用｜后端万能提示词】
直接复制粘贴给 OpenCode / 豆包编程 / Cursor，**永久通用**
适配：小程序后端、接口、管理后台、业务系统
核心目的：**Python快速开发 + 结构规范 + 后期一键转C#/Java/Go**

---

## 一、通用强制规范（必带）
```
请使用 Python + FastAPI 开发，
1. 严格分层：路由层、业务服务层、数据模型层 分离；
2. 采用标准 RESTful 接口，统一 JSON 入参、JSON 响应；
3. 数据库逻辑、业务逻辑、接口路由完全解耦；
4. 代码注释清晰、命名规范、不要写臃肿嵌套；
5. 不要硬编码配置，方便后期迁移重构；
6. 禁止耦合冗余代码，保证模块独立，方便后续无缝转为 C# / Java / Go。
```

---

## 二、需要数据库时，加这段
```
使用 MySQL 数据库，ORM 方式操作，表结构单独抽离，
SQL 逻辑统一管理，方便后续跨语言迁移。
```

---

## 三、需要小程序/前端对接，加这段
```
接口跨域放行，返回格式统一：
{code:状态码, msg:提示信息, data:业务数据}
保证前端、小程序直接调用，无需额外改造。
```

---

## 四、后期「Python 转其他语言」一键提示词
哪天业务变大、想要换性能更强的语言，直接发这段：
```
保持现有所有接口地址、入参、出参、数据库结构、业务逻辑完全不变，
将当前 Python 代码，完整重构为【C# .NET / Go / Java】版本，
分层架构不变，功能完全对齐，可直接替换上线。
```

---

# 五、整套逻辑极简总结
1. 现在：**AI + Python**，极速写功能、快速上线试错
2. 中期：接口不动，AI 单独把高频接口改成 C#/Go
3. 后期：全量核心服务重构，Python 只留 AI、数据、脚本

全程不用自己硬啃语法，**AI 包办迁移**，是目前单人开发性价比最高的路线。



1. p_easyQuiz 
   - 运行方式：本地运行
   - 运行方式：通过Node.js http-server运行
   - 数据：内置在js/题库/目录下的JSON文件中
   - API：调用豆包API
2. p_file_summarizer
   - 运行方式：本地运行
   - 运行方式：通过Flask Python服务器运行（端口5000）
   - 数据：用户上传文件
3. p_hiragana
   - 运行方式：本地运行（直接打开HTML）
   - 运行方式：浏览器直接打开index.html
   - 数据：内置在vocab_data.js和kata-hiragana-deepseek.js文件中，以及index.html中的kataContentDataBuiltIn和hiraganaDataBuiltIn数组
4. p_AI_zixunshi
   - 运行方式：本地运行（直接打开HTML）
   - 运行方式：浏览器直接打开index.html
   - 数据：无内置，通过调用豆包API生成题目
5. p_成语天空
   - 运行方式：本地运行（直接打开HTML）
   - 运行方式：浏览器直接打开index.html
   - 数据：内置在index.html中的_oldChengyuData数组，也尝试从成语库.json读取
   - API：调用豆包API
6. p_humor_h5
   - 运行方式：本地运行（直接打开HTML）
   - 运行方式：浏览器直接打开index.html
   - 数据：内置在js/data.js文件中
7. p_humor_h5_vc
   - 运行方式：本地运行（直接打开HTML）
   - 运行方式：浏览器直接打开index.html
   - 数据：内置在js/data.js文件中
8. p_happyQuiz
   - 运行方式：本地开发
   - 运行方式：通过uni-app开发环境运行（npm run dev:h5）
   - 数据：使用Supabase云端数据库
9. p_shinefore_vc
   - 运行方式：本地开发
   - 运行方式：通过Next.js开发环境运行（npm run dev）
   - 数据：需要进一步检查
现在我有足够的信息来总结所有 p_ 文件夹中项目的运行方式和数据来源了。以下是详细分析：
---
所有 p_ 项目运行方式汇总
1. p_easyQuiz
- 运行方式：本地运行
- 运行方式：Node.js http-server（npm start）
- 数据方式：内置，在 js/题库/ 目录下的 JSON 文件中（quiz-config.json, quiz-doubao.json 等）
- API：调用豆包 API（通过 base64 编码的 API Key）
2. p_file_summarizer
- 运行方式：本地运行
- 运行方式：Flask Python 服务器（python app.py，端口 5000）
- 数据方式：用户上传文件（通过 POST /upload 接口）
3. p_hiragana
- 运行方式：本地运行
- 运行方式：浏览器直接打开 index.html
- 数据方式：内置
  - vocab_data.js - 汉字数据
  - kata-hiragana-deepseek.js - 平假名数据
  - kata-attach.json - 片假名黏附词
  - index.html 中的 kataContentDataBuiltIn 和 hiraganaDataBuiltIn 数组
4. p_AI_zixunshi
- 运行方式：本地运行
- 运行方式：浏览器直接打开 index.html
- 数据方式：无内置，动态调用豆包 API 生成个性化测试题
5. p_成语天空
- 运行方式：本地运行
- 运行方式：浏览器直接打开 index.html
- 数据方式：内置
  - 优先尝试读取 成语库.json 文件
  - 降级使用 index.html 中的 _oldChengyuData 数组（内置约53个成语）
- API：调用豆包 API（用于创意模式造句/故事）
6. p_humor_h5
- 运行方式：本地运行
- 运行方式：浏览器直接打开 index.html
- 数据方式：内置在 js/data.js 文件中（约735道题目）
7. p_humor_h5_vc
- 运行方式：本地运行
- 运行方式：浏览器直接打开 index.html
- 数据方式：内置在 js/data.js 文件中
8. p_happyQuiz
- 运行方式：本地开发（可发布为 H5 或微信小程序）
- 运行方式：uni-app 开发环境（npm run dev:h5）
- 数据方式：云端 Supabase 数据库（@supabase/supabase-js）
9. p_shinefore_vc
- 运行方式：本地开发
- 运行方式：Next.js 开发环境（npm run dev）
- 数据方式：需要进一步检查 public 目录
10. p_PHDtest
- 运行方式：本地运行
- 运行方式：浏览器直接打开 phd_test.html
- 数据方式：内置在 PHDtest1.txt, PHDtest2.txt, PHDtest3.txt 文件中
11. p_japanese
- 运行方式：需要进一步检查
12. p_chat
- 运行方式：本地运行（直接打开 HTML）
- 数据方式：内置在多个 txt 文件中（胤荣史料.txt, 词汇.txt, 行业评价等目录）
13. p_douban_rating
- 运行方式：本地运行
- 数据方式：读取 xlsx 文件（豆瓣评分数据）
14. p_xlsx
- 运行方式：本地运行
- 数据方式：读取 xlsx 文件
---
总结
项目	运行方式	运行平台	数据来源
大部分 p_ 项目	本地	浏览器直接打开 HTML	内置在 JS/HTML 文件中
p_easyQuiz	本地	Node.js http-server	内置 JSON 文件 + 外部 API
p_file_summarizer	本地	Flask Python 服务器	用户上传文件
p_happyQuiz	本地开发	uni-app	云端 Supabase
p_shinefore_vc	本地开发	Next.js	待查
p_AI_zixunshi / p_成语天空	本地	浏览器直接打开	内置 + 外部 API 调用