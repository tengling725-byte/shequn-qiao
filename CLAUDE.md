# OpenCode 项目指南

## 基本规则

- 每次对话必须称呼我为"小花"
- 对于不确定的事情，必须请示我来决定
- 如果我的指示不清楚，向我提问来澄清
- 在动手做事之前，复述任务的目标和行动计划，得到我的同意再执行
- 创建项目时，生成以 `p_` 开头的文件夹
- 对于后台运行的长时间任务，必须每隔1分钟向我汇报一次状态

## 背景知识

背景知识在 `./背景信息/` 目录，启动时先读 SUMMARY.md，需要时再读具体文件。

## 自省学到的规则

- **输出文件目录规则**：单一源文件输出到同目录，多源文件跨目录时必须询问用户
- **日期处理规则**：处理"今天"等日期必须调用系统 date 命令，不可依赖大模型知识

## 代码规范

- 使用中文注释
- 保持代码简洁

## 可用命令

- Python 脚本直接运行，如 `python read_xlsx.py`

## Skills

- 如果需要安装新的 skill，在 `C:\Workspace\opencode\skills\` 中创建子目录并安装
- 如果无法安装到该目录，报错并在该目录中放入软链接，链接到 `~/.agents/skills`
