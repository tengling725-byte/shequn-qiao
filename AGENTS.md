# AGENTS.md - 项目配置

# 基本规则
每次跟我对话的时候，必须叫我"小花猫"
互动规则:每次对话，必须称呼我为"小花猫"
确认规则:对于不确定的事情，必须请示我来决定
提问规则:如果我的指示不清楚，向我提问来澄清
复述规则:在动手做事之前，复述任务的目标和行动计划，得到我的同意再执行
创建项目规则：创建项目时，请生成一个以"p_"开头的文件夹
汇报规则:对于后台运行的长时间任务，必须每隔1分钟向我汇报一次状态
反省规则:被纠正后反思原因，将防再犯规则写入‘./AI指引/目录，并在AGENTS.md「自省学到的规则」章节添加一句话引用。
#背景知识
背景知识在’./背景信息/目录，启动时先读SUMMARY.md，需要时再读具体文件。

# 自省学习到的规则

## 输出文件目录规则
**单一源文件输出到同目录，多源文件跨目录时必须询问用户。** [详情](./AI指引/
输出文件目录规则.md)

## 日期处理规则
**处理"今天"等日期必须调用系统date命令，不可依赖大模型知识。**[详情](./AI指引/日期处理规则.md)

## 搜索链接规则
**获取链接后先测试反爬，测试通过直接使用；测试失败提示用户并请求提供正确链接。**

## 源文件夹重命名规则
- 处理中：命名加 `_处理中_日期`
- 处理完只剩临时文件：命名加 `_已处理_日期`
- 处理完空掉：命名加 `_已空_日期`
- 完全移动：可以不改名字

## 日志文件要求
- 每次文件整理完成后，在目标目录生成日志文件记录整理内容

## Cloudflare Pages 部署规则
- **静态站点部署：先创建项目再上传 `wrangler pages project create`，上传用 `--commit-dirty=true` 处理未提交状态**
- **域名配置：用当前DNS更简单，只需添加CNAME记录指向 `.pages.dev` 地址**

## 可用命令

- Python 脚本直接运行，如 `python read_xlsx.py`

## 代码规范

- 使用中文注释
- 保持代码简洁

## Skills

- 如果需要安装新的skill，请在C:\Workspace\opencode\skills\ 中创建子目录并安装
## - 如果无法安装到C:\Workspace\opencode\skills\，请报错，在C:\Workspace\opencode\skills\ 中放入软链接，链接到~/.agents/skills

##小程序账号
小条记AppID: 
梗王争霸AppID：

# AI Key
deepseek API Key: sk-07c6ba7b8ff84a458fffa6292622a353