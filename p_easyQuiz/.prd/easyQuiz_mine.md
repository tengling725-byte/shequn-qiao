## 一、需求概述

你的 EasyQuiz 应用需要增加三个**需要登录才能使用**的功能：

| 功能 | 说明 |
|------|------|
| **我的题库** | 查看用户自己上传/创建的题库列表，支持增删改查 |
| **我的历史记录** | 查看用户的答题历史（时间、得分、正确率等） |
| **我的错题本** | 查看用户做错的题目，支持复习和移除 |

### 核心行为

1. **未登录时**：点击这三个功能入口（按钮/链接）时，**不跳转到对应页面**，而是：
   - 弹出提示：“请先登录后使用此功能”
   - 自动跳转或弹出登录窗口（建议跳转到登录页，用户体验更清晰）
   - 登录成功后，自动返回到刚才想访问的功能页面

2. **已登录时**：正常进入对应页面，数据从云端 API 加载

3. **未登录 + 直接访问 URL**：如果用户通过 URL 直接进入这些页面（如 `/#/my-quizzes`），也需要检查登录状态，未登录则重定向到登录页

---

## 二、测试用例规划

### 2.1 功能测试（共 15 个用例）

#### 我的题库 (My Quizzes)

| ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|----|---------|---------|---------|---------|
| MQ-01 | 未登录点击入口 | 用户未登录 | 点击「我的题库」按钮 | 弹出提示“请先登录”，跳转到登录页 |
| MQ-02 | 已登录点击入口 | 用户已登录 | 点击「我的题库」按钮 | 进入题库列表页，显示用户上传的题库 |
| MQ-03 | 空状态展示 | 已登录，无任何题库 | 进入「我的题库」 | 显示“暂无题库，去上传”的引导文案 |
| MQ-04 | 题库列表展示 | 已登录，有至少1个题库 | 进入「我的题库」 | 正确显示题库名称、题目数量、创建时间 |
| MQ-05 | 删除题库 | 已登录，题库列表中有数据 | 点击某个题库的「删除」按钮 → 确认 | 题库从列表中消失，后端同步删除 |
| MQ-06 | 直接访问 URL（未登录） | 未登录 | 直接访问 `/#/my-quizzes` | 重定向到登录页，登录后跳转回我的题库 |
| MQ-07 | 直接访问 URL（已登录） | 已登录 | 直接访问 `/#/my-quizzes` | 正常显示我的题库页面 |

#### 历史记录 (History)

| ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|----|---------|---------|---------|---------|
| HI-01 | 未登录点击入口 | 用户未登录 | 点击「历史记录」按钮 | 弹出提示“请先登录”，跳转到登录页 |
| HI-02 | 已登录点击入口 | 用户已登录 | 点击「历史记录」按钮 | 进入历史记录页，显示答题历史 |
| HI-03 | 空状态展示 | 已登录，无任何答题记录 | 进入「历史记录」 | 显示“暂无答题记录” |
| HI-04 | 历史列表展示 | 已登录，有至少1条记录 | 进入「历史记录」 | 正确显示：题库名称、得分、正确率、答题时间 |
| HI-05 | 点击历史详情 | 已登录，有记录 | 点击某条历史记录 | 进入该次答题的详情页（展示每道题的答案和解析） |
| HI-06 | 直接访问 URL（未登录） | 未登录 | 直接访问 `/#/history` | 重定向到登录页 |

#### 错题本 (Error Book)

| ID | 测试场景 | 前置条件 | 操作步骤 | 预期结果 |
|----|---------|---------|---------|---------|
| EB-01 | 未登录点击入口 | 用户未登录 | 点击「错题本」按钮 | 弹出提示“请先登录”，跳转到登录页 |
| EB-02 | 已登录点击入口 | 用户已登录 | 点击「错题本」按钮 | 进入错题本页，显示错题列表 |
| EB-03 | 空状态展示 | 已登录，无任何错题 | 进入「错题本」 | 显示“暂无错题，继续加油” |
| EB-04 | 错题列表展示 | 已登录，有错题 | 进入「错题本」 | 正确显示：题目内容、用户错误答案、正确答案、解析 |
| EB-05 | 移除错题 | 已登录，错题列表中有数据 | 点击某道错题的「移除」按钮 → 确认 | 错题从列表中消失，后端同步删除 |
| EB-06 | 复习错题 | 已登录，有错题 | 点击「开始复习」按钮 | 进入答题模式，只包含错题 |
| EB-07 | 直接访问 URL（未登录） | 未登录 | 直接访问 `/#/errors` | 重定向到登录页 |

---

### 2.2 单元测试（共 6 个用例）

测试 `app.js` 中的登录守卫逻辑：

| ID | 测试场景 | Mock 状态 | 调用方法 | 预期 |
|----|---------|----------|---------|------|
| UT-01 | 未登录时守卫拦截 | `isLoggedIn: false` | `requireLogin('myQuizzes')` | 触发 `navigate('login')`，设置 `redirectAfterLogin: 'myQuizzes'` |
| UT-02 | 已登录时放行 | `isLoggedIn: true` | `requireLogin('myQuizzes')` | 触发 `navigate('myQuizzes')` |
| UT-03 | 未登录直接访问 URL | `isLoggedIn: false` | 页面初始化时判断路由 | 重定向到登录页，保存目标路径 |
| UT-04 | 登录成功后跳转 | 登录成功 | `afterLogin()` | 跳转到 `redirectAfterLogin` 保存的页面 |
| UT-05 | 登出后清除守卫状态 | 登出操作 | `handleLogout()` | `redirectAfterLogin` 重置为 `null` |
| UT-06 | API 401 时的全局拦截 | API 返回 401 | 任意 API 调用 | 清空登录状态，跳转登录页 |

---

### 2.3 E2E 测试（Playwright）（共 8 个用例）

| ID | 测试场景 | 操作步骤 | 预期结果 |
|----|---------|---------|---------|
| E2E-01 | 完整登录后访问我的题库 | 访问首页 → 点击我的题库（未登录弹窗）→ 点击登录 → 输入凭证 → 登录成功后自动跳转到我的题库 | 我的题库页面正常显示 |
| E2E-02 | 未登录访问历史记录 | 访问首页 → 点击历史记录 → 弹窗提示 → 点击确认 → 跳转登录页 | 停留在登录页，登录后跳回历史记录 |
| E2E-03 | 未登录访问错题本 | 同上，验证错题本入口 | 同上 |
| E2E-04 | 登出后守卫恢复 | 登录 → 访问我的题库 → 登出 → 再次点击我的题库 | 未登录提示再次出现 |
| E2E-05 | 直接 URL 登录守卫 | 未登录直接访问 `/#/my-quizzes` | 自动跳转到登录页 |
| E2E-06 | 登录成功后跳回 | 从错题本触发登录 → 登录成功 → 自动返回错题本 | 错题本页面正常显示 |
| E2E-07 | 数据与云端同步 | 登录后错题 → 切换到另一台设备登录 → 错题本显示相同数据 | 数据一致 |
| E2E-08 | 空状态 UI | 新注册用户访问我的题库、历史记录、错题本 | 三个页面都显示空状态引导文案 |

---

## 三、实现建议（供开发参考）

### 3.1 添加路由守卫

```javascript
// app.js 中增加
const routeGuard = (page, callback) => {
  if (!QuizApp.state.isLoggedIn) {
    alert('请先登录后使用此功能');
    QuizApp.state.redirectAfterLogin = page;
    QuizApp.navigate('login');
    return false;
  }
  callback();
  return true;
};

// 使用示例
'<a href="#" onclick="routeGuard(\'myQuizzes\', () => QuizApp.navigate(\'myQuizzes\'))">我的题库</a>'
```

### 3.2 API 401 统一处理

```javascript
// api.js 的 request 方法中增加
if (response.status === 401) {
  // 清除本地登录状态
  localStorage.removeItem('user');
  // 触发全局事件，让 app.js 知道需要跳转登录
  window.dispatchEvent(new CustomEvent('unauthorized'));
  throw new Error('登录已过期，请重新登录');
}
```

---

## 四、测试执行清单

| 阶段 | 任务 | 工具 |
|------|------|------|
| 开发时 | 写单元测试覆盖守卫逻辑 | Vitest |
| 提交前 | 跑单元测试（UT-01 ~ UT-06） | `npm test` |
| 部署前 | 跑 E2E 测试（E2E-01 ~ E2E-08） | Playwright |
| 上线后 | 手动回归冒烟测试 | 人工验证 |


⚠️ 需要补充的 3 个测试场景
补充场景	问题	建议
网络异常	已登录但 API 请求超时/失败	应显示“网络异常，请稍后重试”，不清空登录状态
Token 过期	登录态过期但前端 isLoggedIn 仍为 true	API 返回 401 时清空前端状态并跳转登录
并发登录	同一账号在两台设备登录	后登录是否踢掉前一个？需要明确设计


好的，帮你提炼成**清晰、可执行的需求描述**，方便你后续自己实现或让AI帮你改代码。

---

## 需求名称：用户数据隔离 + 登录后云端优先

### 一、核心原则（一句话）

> **登录用户的数据只看云端，未登录用户的数据只看本地（不跨用户，不自动合并）。**

---

### 二、规则拆解

| 用户状态 | 数据来源 | 写入位置 | 说明 |
|---------|---------|---------|------|
| **已登录** | 只读云端 API | 仅写入云端 | 不读 localStorage，不降级到本地 |
| **未登录** | 只读 localStorage | 仅写入 localStorage | 不读云端，所有数据本地临时保存 |
| **登录后** | 不主动合并未登录时的本地数据 | — | **不做自动合并**（可选：用户手动导入） |
| **退出登录** | 清空前端 UI 数据 | — | 不清除本地 localStorage，下次未登录时可继续用 |

---

### 三、功能维度的具体行为

#### 我的题库

| 状态 | 行为 |
|------|------|
| 未登录 | 可查看/增删本地题库（存在 localStorage），提示“登录后可永久保存” |
| 已登录 | 查看/增删云端题库，不读本地 |
| 登录后 | 不自动导入本地题库（提供“导入本地题库”按钮让用户主动操作） |

#### 历史记录

| 状态 | 行为 |
|------|------|
| 未登录 | 不记录历史（或仅内存临时记录，不落盘），UI 提示“登录后开始记录” |
| 已登录 | 记录到云端，正常展示 |

#### 错题本

| 状态 | 行为 |
|------|------|
| 未登录 | 不记录错题，提示“登录后自动记录” |
| 已登录 | 记录到云端，正常展示 |

---

### 四、边界情况处理

| 场景 | 处理方式 |
|------|---------|
| 已登录 + 网络请求失败 | 显示“网络错误，请稍后重试”，**不清空 UI 数据**，**不写入 localStorage** |
| 未登录 + 调用 API | 前端直接拦截（不发送请求），提示“请先登录” |
| 登录过程中 Token 过期 | API 返回 401 → 跳到登录页，清空前端登录状态 |
| 已登录设备切换 | 数据完全从云端加载，不受另一设备影响 |

---

### 五、storage.js 需要改的地方（直接可执行）

```javascript
const QuizStorage = {
  // 新增：判断是否已登录
  _isLoggedIn() {
    return !!(Auth && Auth.isLoggedIn && Auth.isLoggedIn());
  },

  async getHistory() {
    if (this._isLoggedIn()) {
      // 已登录：只走云端
      const data = await API.getHistory();
      return data.history || [];
    }
    // 未登录：返回空数组（不记录本地历史）
    return [];
  },

  async addHistory(record) {
    if (this._isLoggedIn()) {
      await API.saveHistory(record);
    }
    // 未登录：什么都不做
  },

  async getErrors() {
    if (this._isLoggedIn()) {
      const data = await API.getErrors();
      return data.errors || [];
    }
    return [];
  },

  async addErrors(question, userAnswer, isCorrect) {
    if (isCorrect) return;
    if (this._isLoggedIn()) {
      await API.saveError(question);
    }
  },

  async getCustom() {
    if (this._isLoggedIn()) {
      const data = await API.getQuizzes();
      return data.quizzes || [];
    }
    // 未登录：从 localStorage 读（可选，如果你允许未登录试用）
    const local = localStorage.getItem('easyQuiz_custom');
    return local ? JSON.parse(local) : [];
  },

  async saveCustom(name, data) {
    if (this._isLoggedIn()) {
      await API.saveQuiz(name, data);
    } else {
      // 未登录：存本地
      const custom = await this.getCustom();
      custom.push({ name, data });
      localStorage.setItem('easyQuiz_custom', JSON.stringify(custom));
    }
  }
};
```

---

### 六、一句话总结需求

> **登录用户 = 云端专属读写；未登录用户 = 本地临时试用（不混淆、不自动合并）。**

这个需求提炼出来之后，你可以：
- 拿去和 Claude Code 对齐实现
- 作为测试用例的验收标准
- 以后扩展到多端同步时也不会乱

